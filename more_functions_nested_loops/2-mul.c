#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to multiply
 * Return: The result of the multipication between a param and b param
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
